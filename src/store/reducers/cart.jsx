// src/store/reducers/cart.js
const initialState = {
    items: [], // Initial empty array or load from localStorage
  };
  
  const cartReducer = (state = initialState, action) => {
    // Handle cart-related actions here
    return state;
  };
  
  export default cartReducer;
  